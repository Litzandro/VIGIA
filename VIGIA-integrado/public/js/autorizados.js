(function(){
  const form=document.getElementById('authorizedForm'); if(!form)return;
  let photo='';
  async function imageData(file){if(!file)return'';const src=await new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(r.result);r.onerror=no;r.readAsDataURL(file)});const img=await new Promise((ok,no)=>{const i=new Image();i.onload=()=>ok(i);i.onerror=no;i.src=src});const max=480,scale=Math.min(1,max/img.width),c=document.createElement('canvas');c.width=Math.round(img.width*scale);c.height=Math.round(img.height*scale);c.getContext('2d').drawImage(img,0,0,c.width,c.height);return c.toDataURL('image/jpeg',.55)}
  document.getElementById('auPhoto').onchange=async e=>{photo=await imageData(e.target.files[0]);showToast('Fotografía lista')};
  const typeLabel={bus_escolar:'Bus escolar',familiar:'Familiar',servicio_domestico:'Servicio doméstico',proveedor:'Proveedor',transporte:'Transporte',otro:'Otro'};
  async function load(){const box=document.getElementById('authorizedList');try{const r=await VigiaAPI.request('/personas-autorizadas?limit=200&sort=fecha_creacion:desc');const rows=r.data||[];box.innerHTML=rows.length?'':'<div class="empty-state">Aún no hay personas autorizadas.</div>';rows.forEach(x=>{const days=Array.isArray(x.dias_semana_json)?x.dias_semana_json.join(', '):'Todos';const el=document.createElement('div');el.className='queue-item';el.innerHTML=`<div class="queue-number"><i class="bi bi-person-check-fill"></i></div><div class="queue-copy"><b>${escapeHtml(x.nombre_completo)}</b><span>${typeLabel[x.tipo]||x.tipo} · ${escapeHtml(x.empresa||x.placa_vehiculo||'Sin empresa/placa')}</span><span>${escapeHtml(x.hora_desde||'00:00')}–${escapeHtml(x.hora_hasta||'23:59')} · Días: ${escapeHtml(days)}</span></div><div class="queue-actions"><span class="badge ${x.estado==='activa'?'ok':'pending'}">${escapeHtml(x.estado)}</span><button class="btn btn-ghost" data-id="${x.id}"><i class="bi bi-trash"></i></button></div>`;el.querySelector('button').onclick=async()=>{if(!confirm('¿Desactivar esta autorización?'))return;await VigiaAPI.request(`/personas-autorizadas/${x.id}`,{method:'DELETE'});showToast('Autorización desactivada');load()};box.appendChild(el)});}catch(e){box.innerHTML=`<div class="empty-state">${escapeHtml(e.message)}</div>`}}
  form.onsubmit=async e=>{e.preventDefault();const days=[...document.querySelectorAll('#auDays input:checked')].map(x=>Number(x.value));try{await VigiaAPI.request('/personas-autorizadas',{method:'POST',body:JSON.stringify({tipo:document.getElementById('auType').value,nombre_completo:document.getElementById('auName').value.trim(),numero_documento:document.getElementById('auDocument').value.trim()||null,empresa:document.getElementById('auCompany').value.trim()||null,placa_vehiculo:document.getElementById('auPlate').value.trim()||null,dias_semana_json:days,hora_desde:document.getElementById('auFrom').value||null,hora_hasta:document.getElementById('auTo').value||null,foto_url:photo||null,estado:'activa'})});form.reset();photo='';showToast('Autorización guardada');load()}catch(err){showToast(err.message,'bi-exclamation-triangle-fill')}};
  document.getElementById('reloadAuthorized').onclick=load;document.getElementById('newAuthorized').onclick=()=>document.getElementById('auName').focus();load();
})();
