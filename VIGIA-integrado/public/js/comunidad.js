(function(){const feed=document.getElementById('postsFeed'),form=document.getElementById('postForm');if(!feed||!form)return;const input=document.getElementById('postText'),count=document.getElementById('postCount'),session=VigiaAPI.getSession();input.addEventListener('keydown',e=>{if(e.key==='Enter'&&(input.value.match(/\n/g)||[]).length>=3)e.preventDefault()});input.addEventListener('input',()=>{input.value=input.value.replace(/\n{3,}/g,'\n\n');count.textContent=`${input.value.length}/300`});const badges={'General':'neutral','Objetos perdidos':'warn','Compra-venta':'ok','Aviso importante':'alert'};function moderate(text){const links=(text.match(/https?:\/\//gi)||[]).length;if(links>2)return{ok:false,msg:'La publicación parece spam: máximo 2 enlaces.'};if(/(.)\1{12,}/.test(text))return{ok:false,msg:'Reduce las repeticiones antes de publicar.'};return{ok:true}}function render(rows){feed.innerHTML=rows.length?'':'<div class="empty-state">No hay publicaciones visibles.</div>';rows.forEach(x=>{const card=document.createElement('div');card.className='panel post-card';const mine=String(x.usuario_id)===String(session.id);card.innerHTML=`<div class="post-head"><div class="post-av">${mine?'YO':'VG'}</div><div class="post-who"><b>${mine?'Tú':'Vecino / administración'}</b><span class="mono">${new Date(x.fecha_creacion).toLocaleString('es-HN')}</span></div><span class="badge ${badges[x.categoria]||'neutral'} post-category">${escapeHtml(x.categoria)}</span>${x.visibilidad==='administracion'?'<span class="badge neutral"><i class="bi bi-lock-fill"></i> Privada</span>':''}</div><p class="post-text">${escapeHtml(x.contenido)}</p>${mine?`<div class="post-actions"><button class="post-like" data-delete="${x.id}"><i class="bi bi-trash"></i> Eliminar</button></div>`:''}`;const b=card.querySelector('[data-delete]');if(b)b.onclick=async()=>{await VigiaAPI.request(`/publicaciones-comunidad/${x.id}`,{method:'DELETE'});showToast('Publicación retirada');load()};feed.appendChild(card)})}async function load(){try{const r=await VigiaAPI.request('/publicaciones-comunidad');render(r.data||[])}catch(e){feed.innerHTML=`<div class="empty-state">${escapeHtml(e.message)}</div>`}}form.onsubmit=async e=>{e.preventDefault();const text=input.value.trim(),check=moderate(text);if(!check.ok){showToast(check.msg,'bi-shield-exclamation');return}try{await VigiaAPI.request('/publicaciones-comunidad',{method:'POST',body:JSON.stringify({categoria:document.getElementById('postCategory').value,contenido:text,visibilidad:document.getElementById('postPrivate').checked?'administracion':'residencial'})});form.reset();count.textContent='0/300';showToast(document.getElementById('postPrivate').checked?'Publicación privada enviada':'Publicación compartida');load()}catch(err){showToast(err.message,'bi-exclamation-triangle-fill')}};load()})();
